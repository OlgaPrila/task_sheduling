#include "dag_mapper.h"

namespace mapper {
    DAG_node map(const DAG_node &dag, const DAG_node &clusters,
                 weight_function node, weight_function line){
// maps DAG with clusters
    }
}
